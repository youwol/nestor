name: Typescript Build
description: Build a typescript project
author: jdecharne@youwol.com
inputs:
  skip:
    required: false
    description: |
      list of steps to skip : autogen, build, doc, cleanness
    default: ''
runs:
  using: node20
  main: 'dist/action/index.js'
